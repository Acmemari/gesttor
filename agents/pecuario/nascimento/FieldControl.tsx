/**
 * Reexporta o FieldControl do kit compartilhado de fichas (fonte única). Mantido
 * aqui para não quebrar os imports existentes (FichaInclusaoForm, FichaAnimalForm).
 */
export { default } from '../fichas/FieldControl';
